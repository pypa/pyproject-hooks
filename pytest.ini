[pytest]
addopts =
    --strict-config
    --strict-markers
    --flake8
xfail_strict = True
junit_family = xunit2
filterwarnings =
    error
    ignore:SelectableGroups dict interface is deprecated\. Use select\.:DeprecationWarning
